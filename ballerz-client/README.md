### Install the necessary dependencies with the following commands in vs code: 

```
npx ng add @angular/material
npm install --save @angular/flex-layout
npm install --save @ng-bootstrap/ng-bootstrap
```
